{% extends 'adminBase.html.twig' %}

{% block title %}liste des categories {% endblock %}

{% block body %}

    <div class="container">
        <div class="contenue">

            <div class="row" id="main" >
                <div class="col-sm-12 col-md-12 well" id="content">
                    <h1>liste des Categories</h1>
                </div>
            </div>





            <div><a href="{{ path("admin.AddCategorie") }}" class="btn btn-success">ajouter un produit</a></div>

            <br>
            <div class="example raper">







                <table class="table table-striped">

                    <tr>
                        <th>id</th>
                        <th>nom</th>
                        <th>description </th>
                        <th>niveau</th>

                        <th>action</th>
                        <th>action</th>

                    </tr>


                    {%  for Cat in Cat %}
                        <tr>
                            <td>{{ Cat.id }}</td>
                            <td>{{ Cat.nom }}</td>
                            <td>{{ Cat.description }}</td>
                            <td>{{ Cat.niveau }}</td>






                            <td>
                                <a href="{#{{path("produit.update",{'id':produit.id})}}#}" ><button class="btn btn-primary" >metre a jour</button></a>
                            </td>

                            <td> <a href="{#{{path("admin.produit.delete",{'id':produit.id})}}#}" ><button class="btn btn-danger" type="submit">suprimer</button></a></td>

                        </tr>

                    {% endfor %}

                </table>

                <div  style="margin-left: 350px;margin-top: 70px;">
                    <div class="row mt-30">
                        <div class="col">

                          {#  {{ knp_pagination_render( produits) }}#}


                        </div>
                    </div>
                </div>




            </div>

        </div>
    </div>

{% endblock %}
