{% extends 'base.html.twig' %}

{% block title %}liste de produit!{% endblock %}

{% block body %}

<div class="container">
    <div class="contenue">
    <div class="example raper">




        <div class="table table-striped">

            <table class="table table-striped">

                <tr>
                    <th>id</th>
                    <th>image</th>
                    <th>libelle</th>
                    <th>prix</th>
                    <th>action</th>
                    <th>action</th>
                </tr>


                {%  for produit in produits %}
                    <tr>
                        <td><a href="#">{{ produit.id }}</a></td>

                        <td><a href="#"><img src="{{ produit.image }}" height="50" width="50"></a></td>


                        <td><a href="#">{{ produit.libelle }}</a></td>

                        <td><a href="#">{{ produit.prix }}</a></td>



                        <td>
                            <a href="{{path("produit.update",{'id':produit.id})}}" ><button class="btn btn-primary" >metre a jour</button></a>
                        </td>

                        <td> <button class="btn btn-danger" type="submit">suprimer</button></td>

                    </tr>

                {% endfor %}

        </table>





</div>
    </div>
</div>
{% endblock %}